--Para selecionar todos registros(linhas)

SELECT * FROM cliente;

-- Todas as lojas
SELECT * FROM loja;

-- Todas as categorias de veículos
SELECT * FROM veiculo;

--Para selecionar apenas um registro(linha)

SELECT * FROM cliente WHERE id = 1;

SELECT * FROM loja WHERE id = 1;

SELECT * FROM veiculo WHERE id = 1;